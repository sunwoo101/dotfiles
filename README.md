# My Hyprland Dotfiles

# Under development

Do not use yet

To install on Arch:
```bash
git clone https://github.com/sunwoo101/dotfiles.git ~/dotfiles && cd ~/dotfiles && chmod +x install.sh && ./install.sh
```
